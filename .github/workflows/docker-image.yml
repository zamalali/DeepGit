name: Build Docker Image (DeepGit)

on:
  workflow_dispatch:  # allows manual trigger
  push:
    branches:
      - main  # or change to "master" if that's your default branch

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build Docker image
        run: docker build -t deepgit-app .

      # Optional: Push to Docker Hub
      # - name: Log in to Docker Hub
      #   uses: docker/login-action@v2
      #   with:
      #     username: ${{ secrets.DOCKER_USERNAME }}
      #     password: ${{ secrets.DOCKER_PASSWORD }}

      # - name: Push image to DockerHub
      #   run: |
      #     docker tag deepgit-app yourdockerhub/deepgit-app:latest
      #     docker push yourdockerhub/deepgit-app:latest
